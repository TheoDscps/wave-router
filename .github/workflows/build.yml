name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    # WPF/WinForms — needs the Windows runner, not Linux.
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore WaveRouter.slnx

      - name: Build
        run: dotnet build WaveRouter.slnx --configuration Release --no-restore
