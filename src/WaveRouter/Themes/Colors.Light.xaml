<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Backgrounds -->
    <Color x:Key="BgBaseColor">#FFF7F8FA</Color>
    <Color x:Key="BgSurfaceColor">#FFFFFFFF</Color>
    <Color x:Key="BgSurfaceElevatedColor">#FFF0F1F4</Color>
    <Color x:Key="BorderSubtleColor">#FFE2E4E9</Color>

    <!-- Text -->
    <Color x:Key="TextPrimaryColor">#FF15171B</Color>
    <Color x:Key="TextSecondaryColor">#FF5B6070</Color>

    <!-- Accent (Wave Link cyan, darkened for AA contrast on white)
         NOTE: fails 4.5:1 as *text* color on BgSurface — reserve for icons/graphics/fills only,
         never as a text foreground in light mode. See STYLEGUIDE.md. -->
    <Color x:Key="AccentColor">#FF0EA5C4</Color>
    <Color x:Key="AccentHoverColor">#FF0C8CA8</Color>

    <!-- Semantic -->
    <Color x:Key="SuccessColor">#FF1E9E6D</Color>
    <Color x:Key="WarningColor">#FFB4790F</Color>
    <Color x:Key="ErrorColor">#FFC9352C</Color>
    <Color x:Key="InfoColor">#FF0EA5C4</Color>

    <!-- Brushes (bind to the Color resources above so a single dictionary swap re-themes everything) -->
    <SolidColorBrush x:Key="BgBaseBrush" Color="{StaticResource BgBaseColor}" />
    <SolidColorBrush x:Key="BgSurfaceBrush" Color="{StaticResource BgSurfaceColor}" />
    <SolidColorBrush x:Key="BgSurfaceElevatedBrush" Color="{StaticResource BgSurfaceElevatedColor}" />
    <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource BorderSubtleColor}" />

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentHoverBrush" Color="{StaticResource AccentHoverColor}" />

    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}" />

</ResourceDictionary>
