namespace WaveRouter.Infrastructure.Audio.PolicyConfig;

/// <summary>Only the values this app actually checks — see MSDN winerror.h for the full list.</summary>
public enum HRESULT : uint
{
    S_OK = 0x0,
    S_FALSE = 0x1,
}
